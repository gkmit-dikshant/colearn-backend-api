<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Colearn | OTP Verification</title>

    <style>
      body {
        background: #f6f6f6;
        margin: 0;
        padding: 20px;
        font-family: Arial, sans-serif;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background: #ffffff;
        padding: 24px;
        border-radius: 6px;
      }
      .header {
        font-size: 18px;
        font-weight: 600;
        margin-bottom: 12px;
      }
      .otp {
        display: block;
        font-size: 28px;
        letter-spacing: 4px;
        font-weight: 700;
        margin: 18px 0;
        color: #111;
        background: #f1f1f1;
        padding: 12px 16px;
        border-radius: 6px;
        text-align: center;
      }
      .note {
        font-size: 13px;
        color: #666;
        margin-top: 8px;
      }
      .footer {
        margin-top: 20px;
        font-size: 12px;
        color: #999;
      }
      a {
        color: #1a73e8;
        text-decoration: none;
      }
      @media (max-width: 420px) {
        .container {
          padding: 16px;
        }
        .otp {
          font-size: 22px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container" role="article" aria-label="OTP verification">
      <div class="header">Colearn - OTP for Registration</div>

      <p>Hi <%= username || "there" %>,</p>

      <p>Use the otp below to complete your registration:</p>

      <div class="otp" aria-hidden="true"><%= otp %></div>

      <p class="note">
        This code will expire in <%= expiryMinutes || 5 %> minutes.
      </p>
      <div class="footer">
        &copy; <%= new Date().getFullYear() %> CoLearn. All rights reserved.
      </div>
    </div>
  </body>
</html>
